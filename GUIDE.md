# 🥔 potato-kit 사용 설명서 — "나는 이런 사람인데요"

킷을 깔았는데 뭘 해야 할지 모르겠다면 여기서 자기랑 비슷한 사람을 찾자.
전공이 달라도 **상황**이 비슷하면 그 흐름을 그대로 쓰면 된다.

> 🔰 **Claude Code 자체가 처음이라면** — 터미널 여는 법, 슬래시 명령이 뭔지,
> 첫 세션에서 뭘 쳐야 하는지는 [README의 "Claude Code를 한 번도 안 써봤다면"](./README.md#-claude-code를-한-번도-안-써봤다면)
> 절을 먼저 보자. 5분이면 된다.

- [A. 논문을 재현하고 거기서 내 아이디어를 얹고 싶다](#a-논문을-재현하고-거기서-내-아이디어를-얹고-싶다)
- [B. 해커톤에서 데이터를 방금 받았다](#b-해커톤에서-데이터를-방금-받았다)
- [C. 데이터부터 찾아야 한다. 문제도 아직 흐릿하다](#c-데이터부터-찾아야-한다-문제도-아직-흐릿하다)
- [D. 캐글/데이콘 순위를 올리고 싶다](#d-캐글데이콘-순위를-올리고-싶다)
- [E. 논문 더미에서 지형도를 그려야 한다](#e-논문-더미에서-지형도를-그려야-한다)
- [F. 시계열을 다루는데 결과를 못 믿겠다](#f-시계열을-다루는데-결과를-못-믿겠다)
- [G. 현장 데이터로 무언가를 최적화해야 한다](#g-현장-데이터로-무언가를-최적화해야-한다)
- [공통: 발표와 보고서](#공통-발표와-보고서)
- [공통: 사용량 아끼기](#공통-사용량-아끼기)

---

## A. 논문을 재현하고 거기서 내 아이디어를 얹고 싶다

> *"교수님이 이 논문 읽고 우리 데이터에 적용해보라고 하셨다. 저자 깃허브는 있는데
> 환경 세팅부터 막힌다. 재현이 되면 그다음 내 아이디어를 붙이고 싶다."*

**팩**: 전공에 맞는 것 (`medical`, `finance`, `ocean` …)

### 흐름

```
1) /potato-lit-review <논문 주제 또는 제목>
     → 이 논문이 뭘 주장하는지, 선행/후속 연구가 뭔지 먼저 지형 파악
2) /potato-reproduce <논문 PDF 경로 또는 arXiv/DOI>
     → 저자 코드 클론 → 환경 구성 → 원 논문 결과 재현 → 재현 리포트
3) /potato-reproduce 가 끝나면 이렇게 이어서 말한다:
     "이제 우리 데이터 data/mine.csv 에 같은 방법을 적용해줘.
      원 논문과 데이터 특성이 어떻게 다른지 먼저 짚고 시작해."
4) /potato-experiment
     → 원 방법 vs 내가 바꾼 방법 비교. methods-reviewer 가 감사까지 한다
5) /potato-report
```

### 이 흐름의 핵심

재현은 **"돌아갔다"가 끝이 아니다.** `/potato-reproduce`는 원 논문의 수치와 내가 돌린
수치를 나란히 놓은 표를 남긴다. 숫자가 안 맞으면 왜 안 맞는지(시드, 데이터 버전,
전처리 차이)를 먼저 규명하고 넘어간다. 여기를 대충 넘기면 나중에 "내 아이디어가
좋아서 올라간 건지 원래 세팅이 달라서 올라간 건지" 알 수 없게 된다.

아이디어를 얹을 때는 **한 번에 하나만 바꾸는 것**을 권한다. Claude에게
"변경점 하나씩 추가하면서 각각의 기여도를 따로 측정해줘 (ablation)"라고 하면
그렇게 설계해준다.

### 자주 하는 실수

- 저자 코드가 옛날 버전 라이브러리를 쓴다 → `/potato-reproduce`가 conda 환경을 따로 파므로
  내 다른 프로젝트를 망가뜨리지 않는다. 그냥 진행하면 된다.
- 내 데이터가 논문 데이터와 컬럼이 다르다 → 어댑터를 짜는 게 정석이다.
  원 코드를 뜯어고치지 말고 "내 데이터를 원 코드가 기대하는 형태로 바꾸는 층"을 만든다.

---

## B. 해커톤에서 데이터를 방금 받았다

> *"이틀 안에 결과를 내고 발표까지 해야 한다. 데이터는 방금 받았고 뭐가 들었는지도
> 모른다."*

**팩**: 주최 분야에 맞는 것. 의료면 `medical`

### 흐름 (시간순)

```
[받은 직후 — 10분]
  /potato-eda data/train.csv
     → 행·열, 결측, 타깃 분포, 변수별 관계, 이상치를 HTML 리포트로

[방향 잡기 — 20분]
  "이 데이터로 풀 수 있는 문제를 3개만 제안해줘.
   각각 난이도와 예상 소요시간, 심사에서 먹힐 포인트를 같이."
  → 하나 고른다

  (의료·바이오라면) /potato-lit-review <고른 문제>
     → 임상적으로 말이 되는 접근인지 근거 확인. 발표 때 이게 점수가 된다

[만들기 — 몇 시간]
  /potato-experiment
     → 베이스라인부터 세우고 모델 비교까지 자동으로

[마무리]
  /potato-report      → 결과 정리
  /potato-slides      → 발표 자료
```

### 이 흐름의 핵심

해커톤은 **속도가 곧 점수**다. 프로젝트 폴더의 `CLAUDE.md`에 이 한 줄을 넣어두자:

```markdown
이 프로젝트는 해커톤이다. 속도 우선.
- 완벽한 코드보다 돌아가는 결과를 먼저 만든다
- 검증은 제출/발표 직전에 한 번 몰아서 한다
- 리팩터링·추상화·예외 처리는 하지 않는다
```

그러면 Claude가 과하게 꼼꼼해지지 않는다.

**단, 감사는 생략하지 말자.** 데이터 누수 하나로 리더보드 1등이 실격되는 게
해커톤이다. 제출 전에 이 한마디면 된다:

```
methods-reviewer 로 지금까지 한 작업 감사해줘. 누수랑 검증 설계 위주로.
```

### 발표에서 먹히는 것

심사위원은 정확도만 보지 않는다. `/potato-slides`에 이렇게 요청하면 발표 구조가 잡힌다:

```
/potato-slides 심사위원용. 문제정의 → 왜 이 접근인지 → 결과 → 한계와 다음 단계.
숫자는 크게, 방법론 디테일은 부록으로.
```

---

## C. 데이터부터 찾아야 한다. 문제도 아직 흐릿하다

> *"지자체 과제를 맡았다. '청년 정주여건 개선'이라는 말만 있고 데이터도 문제도
> 내가 정해야 한다."*

**팩**: `korea` (+ 전공 팩)

### 흐름

```
1) 문제를 쪼갠다 — 먼저 대화로
     "청년 정주여건이라는 주제를 데이터로 답할 수 있는 질문 5개로 쪼개줘.
      각 질문마다 어떤 데이터가 있어야 답할 수 있는지도 같이."

2) /potato-find-data <쪼갠 질문 중 하나>
     → KOSIS·공공데이터포털·국토부 실거래가·통계지리정보 등에서 후보 탐색
     → 실제로 받을 수 있는지 확인 → 수집 스크립트 작성 → data/ 에 적재
     → 출처·수집일·라이선스를 sources.md 에 기록

3) /potato-eda  → 받은 데이터가 쓸만한지 확인. 여기서 질문을 조정하는 게 정상이다

4) /potato-experiment  또는 그냥 대화로 분석
     → 예측 모델이 필요 없는 과제도 많다. 기술통계 + 지도 시각화로 충분할 때가 있다

5) /potato-report  → 보고서
   /potato-slides  → 보고회 발표
```

### 이 흐름의 핵심

**데이터 수집은 코드로 남긴다.** `/potato-find-data`는 손으로 다운로드받게 하지 않고
파이썬 스크립트를 짠다. 이유는 세 가지다:

1. 나중에 데이터가 갱신되면 다시 돌리면 된다
2. 보고서의 "데이터 출처 및 수집 방법" 절이 저절로 채워진다
3. 심사·검토에서 "이 숫자 어디서 났냐"는 질문에 스크립트를 보여주면 끝난다

**질문이 바뀌는 걸 두려워하지 말자.** 데이터를 열어보고 나서 질문이 바뀌는 건
실패가 아니라 정상적인 과정이다. `plan.md`에 바뀐 이유를 적어두면 그 자체가
보고서의 좋은 재료가 된다.

### 한국 공공데이터에서 자주 쓰는 곳

| 필요한 것 | 어디서 |
| --- | --- |
| 인구·가구·고용·물가 통계 | KOSIS (국가통계포털) |
| 각종 공공 API·파일 | 공공데이터포털 (data.go.kr) |
| 아파트·전월세 실거래가 | 국토교통부 실거래가 공개시스템 |
| 청약·임대주택 공고 | LH·SH 청약센터 |
| 금리·환율·통화량 | 한국은행 ECOS |
| 해양·조위·수온 | 국립해양조사원 (KHOA) |
| 지역 자체 데이터 | 각 시도 빅데이터 포털 (예: 부산 BIG DATA) |

대부분 무료 API 키가 필요하다. `/potato-find-data`가 어디서 받는지 안내해준다.

---

## D. 캐글/데이콘 순위를 올리고 싶다

> *"데이터도 평가지표도 정해져 있다. 점수를 올리는 게 목적이다."*

**팩**: 없어도 된다 (공통 코어만으로 충분)

### 흐름

```
/potato-eda data/train.csv
  → 타깃 분포, 결측 패턴, train/test 분포 차이를 먼저 본다

/potato-experiment
  → 베이스라인 → 피처 엔지니어링 → 모델 비교(LR/RF/XGB/LGBM) → 감사
  → 교차검증 점수와 리더보드 점수의 괴리를 추적한다

/potato-submit
  → 제출 파일 생성 → 점수 기록 → 다음에 뭘 바꿀지 제안
```

### 이 흐름의 핵심

**CV(교차검증) 점수와 리더보드 점수가 따로 놀면 거기서 멈춰야 한다.**
대부분 원인은 셋 중 하나다: 데이터 누수, 검증 설계 오류, train/test 분포 차이.
`/potato-experiment`의 감사 단계가 이걸 잡아준다. 점수를 더 올리는 것보다 이 괴리를
먼저 해결하는 게 결과적으로 빠르다.

**제출 이력을 남기자.** `/potato-submit`은 `submissions.md`에 "무엇을 바꿨고 점수가
어떻게 변했는지"를 기록한다. 대회 후반에 이 기록이 없으면 뭐가 효과가 있었는지
기억나지 않는다.

---

## E. 논문 더미에서 지형도를 그려야 한다

> *"이 분야가 어떻게 발전해왔는지, 누가 핵심이고 어디가 비어 있는지 알아야 한다."*

**팩**: `libsci` (+ 전공 팩)

### 흐름

```
1) /potato-lit-review <분야>
     → 핵심 논문·저자·흐름 1차 정리

2) 인용 네트워크로 확장 — 대화로
     "이 논문들의 인용·피인용을 OpenAlex 로 긁어서 네트워크를 만들어줘.
      연도별로 어떤 주제 클러스터가 뜨고 지는지 보고 싶다."

3) /potato-eda 를 인용 데이터에 적용
     → 연도별 논문 수, 저자 협업 네트워크, 키워드 동시출현

4) /potato-report
     → 서베이 초안 / 연구 공백(gap) 정리
```

### 이 흐름의 핵심

`libsci` 팩이 붙으면 **OpenAlex·Crossref·Semantic Scholar**를 통해 서지 메타데이터를
대량으로 받을 수 있다. `paper-search`가 "논문 내용"을 찾는 도구라면, 이쪽은
"논문들 사이의 관계"를 보는 도구다. 계량서지학(bibliometrics)을 한다면 이게 본체다.

**연구 공백을 찾을 때**는 이렇게 물어보면 좋다:

```
이 클러스터들 사이에서 서로 인용이 거의 없는 두 갈래를 찾아줘.
방법론적으로 연결될 여지가 있는데 아직 아무도 안 한 조합이 있는지.
```

---

## F. 시계열을 다루는데 결과를 못 믿겠다

> *"백테스트 수익률은 좋은데 실전에서는 안 맞는다. 뭔가 잘못된 것 같다."*

**팩**: `finance`

### 흐름

```
/potato-eda <시계열 데이터>
  → 정상성, 계절성, 구조 변화(structural break), 결측 구간

/potato-experiment
  → 중요: 시계열 모드로 돈다. 랜덤 셔플 교차검증을 쓰지 않고
    워크포워드(walk-forward) 검증을 기본으로 한다

감사를 반드시 따로 한 번 더:
  "methods-reviewer 로 시계열 관점에서 감사해줘.
   룩어헤드, 생존편향, 거래비용, 검증 설계 전부."
```

### 이 흐름의 핵심 — 시계열의 함정 4종

일반적인 데이터 누수 말고도 시계열에는 고유한 함정이 있다.
`methods-reviewer`가 이 넷을 집중적으로 본다.

| 함정 | 무엇인가 | 증상 |
| --- | --- | --- |
| **룩어헤드 편향** | 그 시점에 알 수 없던 정보를 썼다 | 백테스트만 비정상적으로 좋다 |
| **생존 편향** | 지금 살아남은 종목/기업만으로 검증했다 | 과거 성과가 과대평가된다 |
| **비현실적 평가** | 체결 가정·리밸런싱 주기가 현실과 다르다 | 실전에서 재현 안 됨 |
| **비용 무시** | 거래비용·슬리피지·세금을 안 뺐다 | 수익이 비용에 먹힌다 |

**"예측 정확도"보다 "의사결정 품질"을 보자.** 방향성 정확도가 조금 낮아도
손실을 덜 보는 모델이 실제로는 더 나은 경우가 많다. 평가지표를 정할 때
Claude에게 "이 문제에서 정확도 대신 봐야 할 지표가 뭔지" 물어보면 짚어준다.

---

## G. 현장 데이터로 무언가를 최적화해야 한다

> *"관측 데이터를 받아서 운영을 개선해야 한다. 예측이 목적이 아니라 결정이 목적이다."*

**팩**: `ocean` (해양·환경 관측) 또는 `industrial` (최적화·시뮬레이션), 둘 다 켜도 된다

### 흐름 — 관측 데이터 쪽

```
/potato-find-data 또는 ocean 팩으로 바로 조회
  → NOAA 조위·해류·부이, Open-Meteo 기상, KHOA(국내), USGS 수문

/potato-eda
  → 시공간 데이터는 일반 EDA와 다르다. 공간 자기상관·계절성을 같이 본다

/potato-experiment
  → 예측 모델 (수온, 조위, 유량 등)
```

### 흐름 — 최적화 쪽

```
/potato-optimize
  1) 문제 정식화 — 무엇을 정하고(결정변수), 무엇을 최소화/최대화하며(목적),
     무엇을 지켜야 하나(제약)
  2) 도구 선택 — 선형/정수계획이면 OR-Tools, 대기·흐름이면 SimPy,
     네트워크면 NetworkX
  3) 해 구하기
  4) 민감도 분석 — 입력이 흔들리면 답이 얼마나 바뀌나
  5) 리포트
```

### 이 흐름의 핵심

최적화에서 제일 흔한 실패는 **모델링이 틀린 것**이지 푸는 게 어려운 게 아니다.
`/potato-optimize`는 코드를 짜기 전에 "결정변수·목적함수·제약"을 말로 먼저 정리하게 한다.
여기서 말이 안 되면 아무리 잘 풀어도 소용없다.

**민감도 분석을 생략하지 말자.** 현장 데이터는 부정확하다. "수요가 10% 틀리면
답이 어떻게 바뀌나"를 같이 보여주는 것이 실무에서는 최적해 자체보다 중요하다.

---

## 공통: 발표와 보고서

### 보고서

```
/potato-report
/potato-report 학술논문 형식으로. 서론-방법-결과-고찰
/potato-report 비전공자용. 수식 빼고 그림 위주로
/potato-report PDF 로 내보내줘
```

### 발표자료

```
/potato-slides
/potato-slides 10분 발표. 슬라이드 8장 이내
/potato-slides 심사위원용. 결과 먼저, 방법은 뒤에
```

둘 다 **분석 결과가 이미 있어야** 제대로 나온다. `results.md`나 노트북이 있으면
그걸 읽고 쓴다. 없으면 먼저 `/potato-experiment`나 `/potato-eda`를 돌리자.

**기본은 마크다운 하나다.** `/potato-report`는 `.md`를 만든 뒤
"논문 형식 PDF로 만들까요? 발표용 PPTX로 만들까요? md로 충분한가요?"를 물어본다.
대답한 대로만 만든다. 처음부터 `/potato-report PDF로`처럼 지정하면 묻지 않고 바로 만든다.
PDF·PPTX 변환은 의존성과 시간을 잡아먹으니 필요할 때만 하자.

---

## 공통: 사용량 아끼기

Claude Pro는 한도가 있다. 해커톤처럼 몰아서 쓸 때 특히 조심하자.

**모델은 Sonnet 으로 두자.** 킷이 설치할 때 이미 그렇게 설정한다.
Pro 구독에서는 Opus 를 쓸 수 없고, Sonnet 이 한도가 훨씬 오래간다.
서브에이전트도 전부 Sonnet 으로 고정돼 있다. `/model` 로 확인할 수 있다.

**아껴 쓰는 법**

- **큰 데이터를 통째로 보여주지 말자.** "이 CSV 내용 다 읽어줘"가 아니라
  "이 CSV를 pandas로 읽어서 요약 통계만 보여줘"라고 하면 토큰이 1/100로 준다.
  스킬들은 이미 이렇게 동작한다.
- **병렬 에이전트를 남발하지 말자.** `/potato-experiment`가 모델 3개를 병렬로 돌리는데,
  급할 때는 "순차로 돌려줘"라고 하면 한도를 덜 쓴다.
- **긴 세션은 잘라 쓰자.** 주제가 바뀌면 `/clear`로 새 세션을 시작하는 게
  누적된 대화를 끌고 가는 것보다 싸다.
- **한도가 임박했다면** 우선순위는 이렇다:
  1순위 결과를 내는 작업(`/potato-experiment`) → 2순위 정리(`/potato-report`) →
  3순위 탐색(`/potato-lit-review`). 탐색은 나중에 해도 된다.

---

## 그래도 모르겠으면

Claude Code를 열고 그냥 말로 물어보자. 킷이 깔려 있으면 스킬 목록을 알고 있다.

```
나 지금 이런 상황인데 (상황 설명), 어떤 스킬을 어떤 순서로 쓰면 될까?
```
