# potato-kit — 연구 운영 규칙

이 문서는 potato-kit 안에서 작업할 때의 기본 규칙이다.
프로젝트 폴더(`projects/<이름>/CLAUDE.md`)에 다른 규칙이 있으면 그쪽이 우선한다.

---

## 1. 세션 시작 프로토콜

새 세션을 열면 편집 전에 이것부터 한다. 비용이 거의 들지 않고 헛수고를 막는다.

1. 현재 폴더에 `plan.md`가 있으면 읽는다 — 목표와 진행 기록이 거기 있다.
2. `results.md`, `sources.md`가 있으면 읽는다 — 이미 나온 결과를 다시 만들지 않기 위해서다.
3. `data/` 폴더의 파일 **목록만** 확인한다. 내용은 필요할 때 코드로 읽는다.
4. 무엇을 할지 한 문장으로 말하고 시작한다.

---

## 2. 연구 워크플로우

기본 흐름은 여섯 단계다. 전부 거칠 필요는 없고, 필요한 데서 시작하면 된다.

```
0. 데이터 발굴   /potato-find-data     ← 데이터가 아직 없을 때만
1. 문헌·근거     /potato-lit-review
2. 탐색          /potato-eda
3. 실험          /potato-experiment
4. 감사          methods-reviewer  ← 3에 포함되지만 따로 부를 수도 있다
5. 산출물        /potato-report  /potato-slides  /potato-submit
```

**단계마다 무엇을 남기나**

| 단계 | 남기는 것 |
| --- | --- |
| 데이터 발굴 | 수집 스크립트 + `sources.md`(출처·수집일·라이선스) |
| 문헌 | `notes/lit-<주제>.md` |
| 탐색 | `reports/eda-<데이터명>.html` |
| 실험 | 재실행 가능한 스크립트 + `results.md` |
| 감사 | `results.md`의 "감사" 절 |
| 산출물 | `reports/` 아래 md/pdf/pptx |

`plan.md`의 "진행 기록"에 날짜와 한 줄 요약을 남긴다. 다음 세션의 나를 위한 것이다.

---

## 3. 데이터 보호 (중요)

**원본 데이터는 로컬 코드 안에서만 다룬다. 모델 컨텍스트로 올리지 않는다.**

| 해도 되는 것 | 하면 안 되는 것 |
| --- | --- |
| 스키마·컬럼명·dtype 확인 | 원본 레코드를 통째로 출력·붙여넣기 |
| 집계 통계(평균·분포·결측률) | 개별 환자/개인 행을 대화에 노출 |
| 익명화·집계된 샘플 몇 행 | 식별정보(이름·주민번호·차트번호·주소) 노출 |
| 모델 성능 지표 | 원본 파일을 외부 API로 전송 |

이유는 두 가지다. 첫째, 의료·개인정보 데이터는 사용 협약(DUA)이나 IRB 조건에
**외부 전송 금지**가 들어 있는 경우가 많다. 둘째, 대화 기록은 남는다.

**적용 대상**: 환자 기록, 임상시험 원자료, 개인식별 가능 행정데이터,
기업 내부 데이터, 미공개 실험 데이터.
공개 데이터(캐글, 공공데이터포털 개방 데이터)는 이 제한을 받지 않는다.

판단이 서지 않으면 사용자에게 물어본다. "이 데이터 외부 반출 제한이 있나요?"

---

## 4. 환경 부트스트랩

분석을 시작하기 전에 환경을 확인한다.

1. `conda --version` — 없으면 Miniconda 설치를 안내한다.
   설치처럼 시스템을 바꾸는 작업은 **반드시 사용자 확인을 받고** 진행한다.
2. `conda env list`에 `potato`가 있는지 확인. 없으면 만든다:
   ```
   conda create -n potato python=3.11 -y
   ```
3. 패키지가 없으면 그 환경에 설치한다:
   ```
   conda run -n potato pip install <패키지>
   ```
4. **모든 파이썬 실행은 이 환경에서 한다**:
   ```
   conda run --no-capture-output -n potato python script.py
   ```
   시스템 파이썬이나 base 환경을 오염시키지 않는다.

프로젝트가 특별한 버전을 요구하면(논문 재현 등) 별도 환경을 판다:
`conda create -n potato-<프로젝트명> python=3.10 -y`

---

## 5. 산출물 규칙

- **모든 실험은 재실행 가능한 스크립트로 남긴다.** 대화창에서만 돌리고 끝내지 않는다.
  스크립트가 없으면 3일 뒤에 그 결과를 다시 만들 수 없다.
- **난수는 시드를 고정한다** (`random_state=42` 등). 시드를 안 박으면 재현이 안 된다.
- **결과는 `results.md`에 표로 기록한다.** 모델명, 지표, 표준편차, 사용한 설정.
- **그림은 파일로 저장한다** (`reports/figs/`). 노트북 안에만 있으면 보고서에 못 쓴다.
- 실패한 시도도 `results.md`에 한 줄 남긴다. 같은 삽질을 반복하지 않기 위해서다.

**산출물은 마크다운이 기본이다.** PDF·PPTX는 묻지 않고 만들지 않는다.
`.md`를 먼저 만들어 보여주고 — "논문 형식 PDF로 만들까요? 발표용 PPTX로
만들까요? 아니면 md로 충분한가요?" — **대답을 받은 뒤에** 변환한다.
사용자가 처음부터 형식을 지정했으면(`/potato-report PDF로`) 다시 묻지 않는다.

이유: 변환은 의존성(pandoc·LaTeX·python-pptx)과 시간을 잡아먹는데,
실제로 필요한 경우는 절반도 안 된다.

### 결정 로그 — 결과만이 아니라 '왜'를 남긴다

`results.md`는 결과를, `plan.md` 진행 기록은 한 줄 요약을 남기지만,
**"왜 그렇게 했는지"는 둘 다에 안 남는다.** 다음 순간마다 결정 로그를 쓴다:

- 방향을 정하거나 바꿀 때 — 무엇을 검토했고 왜 이쪽인가
- 실수를 발견하고 정정할 때 — 무엇이 틀렸고 어떻게 확인했나
- 사용자와의 문답으로 설계가 바뀔 때 — 질문의 요지 + 내 답 + 내려진 결정

**형식**: `로그/NN_제목.md` 순번 파일. 맨 위에 한 줄 요약, 본문에 근거
(수치는 기억이 아니라 산출물 파일에서 읽어 인용). **폐기된 결론은 지우지 말고
폐기 표시를 남긴다** — 나중에 그 로그만 읽은 사람이 낡은 결론을 재인용하는
사고를 막는다.

이 로그의 용도는 둘이다: ① 지도교수·팀원에게 "왜 이렇게 했는지"를 순서대로
설명할 수 있게 한다 (결과 표만으로는 불가능하다), ② 다음 세션의 나에게
plan.md 한 줄보다 진한 컨텍스트 복원 수단이 된다.

대화 전문을 받아 적는 게 아니다 — 세션 원문은 Claude Code가 자체 보관한다.
로그에는 **정제된 결정과 근거만** 담는다. 매 대화가 아니라 결정이 있을 때만 쓴다.

---

## 6. 서브에이전트를 언제 쓰나

| 쓴다 | 직접 한다 |
| --- | --- |
| 여러 모델을 병렬로 학습·평가 | 파일 하나 읽기 |
| 독립적인 감사(코드/데이터)를 동시에 | 순차적으로 의존하는 편집 |
| 넓은 문헌 조사 | 이미 아는 논문 한 편 확인 |

**서브에이전트는 비용이 곱해진다.** 각자 컨텍스트를 새로 만들고 보고서를 쓰고,
그걸 다시 읽어야 한다. 직접 하면 몇 번의 도구 호출로 끝날 일을 위임하지 않는다.

Pro 구독을 쓰는 중이라면 병렬은 3개를 넘기지 않는다.

---

## 7. 사용량 아끼기

- 큰 파일을 통째로 읽지 않는다. `pandas`로 읽어 요약만 본다.
- 탐색은 얕게, 결과 생성은 깊게. 파일을 찾는 데 토큰을 쓰지 않는다.
- 주제가 바뀌면 새 세션을 연다. 긴 대화를 끌고 다니는 게 더 비싸다.
- 한도가 임박하면 우선순위: **결과 생성 > 정리 > 탐색**.

---

## 8. 소통 방식

- 결론을 먼저 말한다. 근거는 그 다음이다.
- 수치를 말할 때는 어디서 나온 수치인지 같이 밝힌다.
- **검증되지 않은 것을 검증된 것처럼 말하지 않는다.** 테스트가 실패했으면
  실패했다고 말하고 출력을 보여준다.
- 사용자가 문제를 설명하거나 질문하는 중이면 고치지 말고 **진단만** 내놓는다.
  고쳐달라고 하면 그때 고친다.

---

## 9. 하지 말아야 할 것

- 원본 데이터 파일을 덮어쓰기 (`data/`는 읽기 전용으로 다룬다. 가공 결과는 새 파일로)
- 성능이 안 나온다고 검증 설계를 느슨하게 바꾸기
- 데이터 누수를 발견하고도 "점수가 잘 나오니까" 넘어가기
- API 키·토큰을 코드나 노트북에 하드코딩하기 (`.env`를 쓰고 `.gitignore`에 넣는다)
- 사용자 확인 없이 시스템 설정 변경, 패키지 전역 설치, 파일 대량 삭제
